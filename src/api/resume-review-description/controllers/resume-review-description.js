'use strict';

/**
 * resume-review-description controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::resume-review-description.resume-review-description');

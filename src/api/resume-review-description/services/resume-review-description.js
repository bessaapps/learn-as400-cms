'use strict';

/**
 * resume-review-description service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::resume-review-description.resume-review-description');
